<?xml version="1.0" encoding="utf-8"?>
<!-- Plain brand background only (matches the in-app SplashScreen bg) so the
     native launch window blends into the Flutter splash with no separate
     logo screen. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_bg" />
</layer-list>
